<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("partials/head") %>
     <link rel="stylesheet" href="/css/style-productDetail.css">    
    </head>
    <body>
        <header>
            <% if (locals.isAdmin) { %>
    
                <%- include("partials/headerProfileAdmin") %>
             
            <% } else if (locals.isLogged) { %>
    
                <%- include("partials/headerProfile") %>
    
            <% } else { %>
    
                <%- include("partials/header") %>
    
            <% } %>
        </header>
    <main>
        
        <h3>Aquí puedes cambiar tus datos personales</h3>
        <img src="/images/userImages/<%= user.image %>" width = 200px>

        <form action="/user/profile/edit" class="user-data" method="POST" enctype="multipart/form-data">
            <input type="file" id="images" name="image" value="<%= user.image %>">
            <br>
            <input type="text" id="firstName" name="firstName" placeholder="<%= user.firstName %>" value="<%= user.firstName %>">
            <br>
            <input type="text" id="price" name="lastName" placeholder="<%= user.lastName %>" value="<%= user.lastName %>">
            <br>
            <input type="text" id="email" name="email" placeholder="<%= user.email %> " value="<%= user.email %>">
            <br>
            <input type="text" id="userName" name="userName" placeholder="<%= user.userName %> " value="<%= user.userName %>">
            <br>
            <!-- <input type="text" id="quota" name="quota" placeholder="Coloque su contraseña> -->
            <br>
            <input type="checkbox" name="newsletter"> Deseo recibir información sobre eventos y promociones.
    
                <div class="edit">
                    <button type="submit">Guardar</button>
                </div>
            </div>
        </form>

    </main>




    <footer>
            <%- include("partials/footer") %>
    </footer>


</body>
</html>